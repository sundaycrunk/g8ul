doctype html
html(lang="en")
  include common/head.pug
  +head("Starter MVP")
  body
    header
      include common/navbar.pug
      +navbar()
      p this is the header
    h1 Welcome to #{title}
    h3= subtitle
    div minimum viable landing page
    |
    |
    form(action="/", method="POST")
      input(name="lead_email" type="email", placeholder="Enter email")
      button(type="submit") Get Started
    //- next step is to make this shit appear as a table.
    if leads
      h1 Leads
        table.table
          thead
            tr
              th(scope="col") Email
              th(scope="col") Actions
              th(scope="col")
            tr
          tbody
            for lead in leads
              tr(id=lead.id)
                td
                  span.orange #{lead.email}
                td
                  a(href="/lead/" + lead.id) Details
                td
                  button(onClick=`deleteLead('${lead.id}')`).btn.btn-primary Delete
    include common/footer.pug
    +footer()
    script(src="https://code.jquery.com/jquery-3.2.1.min.js")
    script(src="/javascripts/leads.js")
  //- p#lblMessage(class=(status  ? 'text-success' : 'text-warning')) #{message}
  //- you can do shit like this. cool.
  //- but how are the vars stored?
